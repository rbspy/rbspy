* don't use process::exit, instead do something else
* install a panic handler that kills the child when starting a child
* implement some of the get_stack_trace_function stuff in a more readable way
* switch back to stable Rust
* research if you can have C code that calls Ruby code

AFTER RELEASE

* Mac support
* Container support
